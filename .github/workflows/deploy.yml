name: Deploy to VPS

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to VPS via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            echo "📦 Обновление кода на сервере..."
            cd /root
            rm -rf fastapi-actions-demo
            git clone https://github.com/${{ github.repository }}.git fastapi-actions-demo
            cd fastapi-actions-demo
            
            echo "🐳 Сборка Docker-образа..."
            docker build -t fastapi-app:latest .
            
            echo "🛑 Остановка старого контейнера..."
            docker stop fastapi-app 2>/dev/null || true
            docker rm fastapi-app 2>/dev/null || true
            
            echo "🚀 Запуск нового контейнера..."
            docker run -d \
              --name fastapi-app \
              -p 8000:8000 \
              --restart unless-stopped \
              fastapi-app:latest
            
            echo "✅ Деплой завершён!"